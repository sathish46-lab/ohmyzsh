# Load colors
autoload -U colors && colors

# Define colors
local sky_blue="%F{cyan}"
local yellow="%F{yellow}"
local cyan="%F{cyan}"
local red="%F{yellow}"      
local white="%F{white}"
local reset="%f"

# Define the prompt
PROMPT='${sky_blue}%n${reset} ${cyan}🦋${reset} ${red}%~${reset} ${cyan}>${rese$
